[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hexmil"
version = "0.1.0"
description = ""
authors = [
  { name="opontorno", email="orazio.pontono@phd.unict.it" }
]
dependencies = [
]

[tool.setuptools.packages.find]
where = ["src"]